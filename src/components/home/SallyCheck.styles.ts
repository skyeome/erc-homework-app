import { styled } from "@mui/material";

const SallyCheckWrap = styled("div")`
  display: flex;
  align-items: center;
  justify-content: flex-end;

  width: 15.5rem;
  margin: 0 auto;

  & img {
    width: 6rem;
  }
`;

export default SallyCheckWrap;
