import { Button, styled } from "@mui/material";

export const RecorderButton = styled(Button)`
  width: 6.625rem;
  height: 6.625rem;
  border-radius: 100%;
  box-shadow: none;
  &:hover,
  &:focus {
    box-shadow: none;
  }
`;
